name: TrailNote Android CI

on:
  push:
    branches: ["trailnote-v1"]
    paths:
      - "TrailNote/**"
      - ".github/workflows/trailnote-android.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Lint and build installable debug APK
        working-directory: TrailNote
        run: gradle :app:lintDebug :app:assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TrailNote-v1-debug-apk
          path: TrailNote/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
